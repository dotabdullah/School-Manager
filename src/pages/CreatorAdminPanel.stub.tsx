// Stub used ONLY in school builds (see vite.config.ts resolve.alias).
// The real CreatorAdminPanel.tsx — and any signing-key handling it contains —
// is never part of the school build's module graph at all when this alias is active.
export default function CreatorAdminPanelStub() {
  return null;
}
